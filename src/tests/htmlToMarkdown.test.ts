import { describe, expect, it } from 'vitest'; import { htmlToMarkdown } from '../core/htmlToMarkdown';
describe('htmlToMarkdown',()=>{ it('converts inline and block elements',()=>{ expect(htmlToMarkdown('<p><strong>Bold</strong> <em>x</em><br><s>gone</s></p>')).toBe('**Bold** *x*\n~~gone~~'); }); it('handles links and lists safely',()=>{ expect(htmlToMarkdown('<a href="javascript:alert(1)">bad</a><a href="https://x.test">ok</a>')).toBe('bad[ok](https://x.test)'); expect(htmlToMarkdown('<ul><li>One</li><li><u>Two</u></li></ul>')).toBe('- One\n- <u>Two</u>'); }); it('converts inline code', () => { expect(htmlToMarkdown('<p>Use <code>npm test</code> to run the suite.</p>')).toBe('Use `npm test` to run the suite.'); }); it('converts preformatted code with a language', () => { expect(htmlToMarkdown('<pre><code class="language-ts">const value = 1;\n</code></pre>')).toBe('```ts\nconst value = 1;\n```'); }); it('uses a longer fence when code contains backticks', () => { const result = htmlToMarkdown('<pre><code>const fence = "```";</code></pre>'); expect(result).toBe('````\nconst fence = "```";\n````'); }); it('preserves html-looking code as text', () => { expect(htmlToMarkdown('<pre><code>&lt;button type="button"&gt;Save&lt;/button&gt;</code></pre>')).toBe('```\n<button type="button">Save</button>\n```'); });});
